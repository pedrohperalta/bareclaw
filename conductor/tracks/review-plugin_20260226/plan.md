# Implementation Plan: Use Code-Review Plugin in PR Review Flow

**Track ID:** review-plugin_20260226
**Spec:** [spec.md](./spec.md)
**Created:** 2026-02-26
**Status:** [x] Complete

## Overview

Update `src/core/github-pr-review.ts` to replace the generic review prompt with a `/code-review:review-pr` invocation, remove the unnecessary PR branch checkout, and update tests.

## Phase 1: Update Review Flow

### Tasks

- [x] Task 1.1: Replace `REVIEW_SYSTEM_PROMPT` with a prompt that instructs Claude to run `/code-review:review-pr {number} --repo {owner}/{repo}`
- [x] Task 1.2: Remove the `git fetch` and `git checkout` steps from `processGitHubPrReview` (keep the clone on the default branch)
- [x] Task 1.3: Update tests in `src/core/github-pr-review.test.ts` to match the new prompt

### Verification

- [x] All tests pass (`npx vitest run`)
- [x] TypeScript compiles (`npx tsc --noEmit`)

## Final Verification

- [x] All success criteria met
- [x] Tests passing

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
